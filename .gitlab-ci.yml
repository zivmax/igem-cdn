image: python:3.12

stages:
  - build

before_script:
  - pip install -r requirements.txt

build:
  stage: build
  script:
    - pip install pyinstaller
    - pyinstaller --name igem-upload --onefile main.py
  artifacts:
    paths:
      - dist/igem-upload
