image: python:3.12

stages:
  - build

before_script:
  - pip install -r requirements.txt

build:
  stage: build
  script:
    - pip list
    - pyinstaller --name igem-upload --onefile main.py --strip
    - du -sh dist/igem-upload
  artifacts:
    paths:
      - dist/igem-upload
