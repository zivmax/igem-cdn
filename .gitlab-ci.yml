image: python:3.12

stages:
  - build

before_script:
  - pip install -r requirements.txt

build:
  stage: build
  script:
    - bash setup.sh build 
    - du -sh dist/igem-upload
  artifacts:
    paths:
      - dist/igem-upload
